[project]
name = "letrava-backend"
version = "0.1.0"
description = "Letrava — letters from real people, written for the future."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --tb=short"
